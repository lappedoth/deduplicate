# deduplicate

A simple Python CL tool tp find and remove duplicate files in a directory, replacing them with symbolic links to the original file.

## Usage

```bash
deduplicate /path/to/directory
```

---
